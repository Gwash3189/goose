<!-- app/views/user_mailer/reset_password_email.html.erb -->
<h1>Reset your password</h1>

<p>Hi <%= @user.full_name.presence || 'there' %>,</p>

<p>We received a request to reset your password. Click the button below to create a new password:</p>

<p style="text-align: center; margin: 30px 0;">
  <a href="<%= @reset_url %>" class="button">Reset Password</a>
</p>

<p>Or copy and paste this link into your browser:</p>
<p class="code"><%= @reset_url %></p>

<div class="alert">
  <strong>⚠️ This link will expire in <%= @expires_in %></strong><br>
  For security reasons, password reset links are only valid for a limited time.
</div>

<p>If you didn't request a password reset, please ignore this email. Your password won't be changed.</p>

<p>
  Best regards,<br>
  The <%= ENV.fetch('APP_NAME', 'YourApp') %> Team
</p>
